<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="notice">

<!-- #####게시판(관리자, 회원) -->
<select resultType="hashMap" parameterType="hashMap" id="selectNoticeList">
<![CDATA[ SELECT NT_NUM, NT_TYPE, NT_TITLE, NT_DATE FROM NOTICE WHERE NT_TYPE='공지유형' ]]>
</select>

<!-- #####글쓰기(관리자) -->
<insert parameterType="hashMap" id="insertNoticeWrite">
<![CDATA[ INSERT INTO NOTICE ( NT_NUM, NT_TYPE, NT_TITLE, NT_CONTENT, NT_DATE ) VALUES (NT_NUM_SEQ.NEXTVAL, #{NT_TYPE}, #{NT_TITLE}, #{NT_CONTENT}, SYSDATE ) ]]>
</insert>
<select resultType="hashMap" parameterType="hashMap" id="selectNoticeDetail">
<![CDATA[ SELECT NT_NUM, NT_TYPE, NT_DATE, NT_TITLE, NT_CONTENT FROM NOTICE WHERE NT_NUM = #{NT_NUM} ]]>
</select>

<!-- #####글 삭제(관리자) -->
<delete parameterType="hashmap" id="deleteNotice">
<![CDATA[ DELETE NOTICE WHERE NT_NUM = #{NT_NUM} ]]>
</delete>
</mapper>